<UserControl x:Class="RecordingsHelper.WPF.Views.TranscriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecordingsHelper.WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:RecordingsHelper.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    
    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding DataContext.NavigateToHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
    </UserControl.InputBindings>
    
    <UserControl.Resources>
        <converters:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter"/>
        <converters:SecondsToTimeStringConverter x:Key="SecondsToTimeStringConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16">
            <DockPanel>
                <Button DockPanel.Dock="Left" 
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding DataContext.NavigateToHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        ToolTip="Back to Home"
                        Foreground="White">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
                
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding OpenSettingsCommand}"
                        ToolTip="Azure Speech Settings"
                        Foreground="White"
                        Margin="8,0,0,0">
                    <materialDesign:PackIcon Kind="Cog" Width="24" Height="24"/>
                </Button>
                
                <TextBlock Text="Audio Transcription" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"/>
                
                <Border Background="#FFF59D" 
                        CornerRadius="12" 
                        Padding="8,4" 
                        Margin="12,0,0,0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Flask" 
                                                 Width="16" 
                                                 Height="16" 
                                                 VerticalAlignment="Center"
                                                 Foreground="#F57F17"
                                                 Margin="0,0,4,0"/>
                        <TextBlock Text="EXPERIMENTAL" 
                                   FontWeight="Bold" 
                                   FontSize="11"
                                   Foreground="#F57F17"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

            <!-- Transcription Controls - Always Visible -->
            <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Transcription Controls" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                                   VerticalAlignment="Center"/>
                        
                        <Button Command="{Binding StartTranscriptionCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="MicrophoneMessage" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Start Transcription"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- Diarization and Profanity Filter -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <CheckBox Content="Diarization"
                                  IsChecked="{Binding EnableDiarization}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0"
                                  ToolTip="Identify different speakers"/>
                        <TextBlock Text="Max Speakers:"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <Slider Value="{Binding MaxSpeakers}"
                                Minimum="2"
                                Maximum="10"
                                Width="120"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                AutoToolTipPlacement="TopLeft"
                                VerticalAlignment="Center"
                                Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding MaxSpeakers}"
                                   VerticalAlignment="Center"
                                   MinWidth="15"
                                   Margin="0,0,24,0"/>
                        
                        <!-- Profanity Filter -->
                        <TextBlock Text="Profanity Filter:"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding ProfanityFilterModes}"
                                  SelectedItem="{Binding ProfanityFilterMode}"
                                  MinWidth="100"
                                  VerticalAlignment="Center"
                                  ToolTip="None: No filtering | Masked: Replace with asterisks | Removed: Remove from result | Tags: Add profanity tags"/>
                    </StackPanel>

                    <!-- LLM Enhancement -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <CheckBox Content="Enable LLM Enhancement"
                                  IsChecked="{Binding UseLlmEnhancement}"
                                  VerticalAlignment="Center"
                                  ToolTip="Use advanced AI model for improved accuracy and formatting"/>
                        
                        <Button Command="{Binding OpenLlmPromptCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Height="28"
                                Padding="8,0"
                                Margin="12,0,0,0"
                                ToolTip="Configure LLM prompt"
                                IsEnabled="{Binding UseLlmEnhancement}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="MessageText" 
                                                         Width="16" 
                                                         Height="16" 
                                                         VerticalAlignment="Center" 
                                                         Margin="0,0,4,0"/>
                                <TextBlock Text="Configure Prompt" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Blob Storage Upload -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <CheckBox Content="Upload to Blob Storage"
                                  IsChecked="{Binding UploadToBlobStorage}"
                                  VerticalAlignment="Center"
                                  ToolTip="Upload file to Azure Blob Storage (allows reuse for batch transcription)"/>
                        
                        <TextBlock Text="{Binding BlobUrl, StringFormat='Blob URL: {0}'}"
                                   FontSize="10"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="250"
                                   ToolTip="{Binding BlobUrl}"
                                   Visibility="{Binding BlobUrl, Converter={StaticResource NullToVisibilityConverter}}"/>
                        
                        <Button Command="{Binding CopyBlobUrlCommand}"
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Copy blob URL to clipboard"
                                Width="24"
                                Height="24"
                                Padding="0"
                                Margin="4,0,0,0"
                                Visibility="{Binding BlobUrl, Converter={StaticResource NullToVisibilityConverter}}">
                            <materialDesign:PackIcon Kind="ContentCopy" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>

                    <ProgressBar Value="{Binding TranscriptionProgress}"
                                 Maximum="100"
                                 Height="4"
                                 Margin="0,12,0,0"
                                 Visibility="{Binding IsTranscribing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- File Selection and Audio Player Card (Combined) -->
            <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="250"/>
                        <ColumnDefinition Width="*" MinWidth="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- File Selection Section -->
                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="Audio File" 
                                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                                   Margin="0,0,0,8"/>
                        
                        <Button Command="{Binding LoadFileCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                IsEnabled="{Binding IsTranscribing, Converter={StaticResource InverseBooleanConverter}}"
                                HorizontalAlignment="Left"
                                Margin="0,0,8,8">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOpen" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Load Audio File"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding ClearCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                IsEnabled="{Binding IsFileLoaded}"
                                HorizontalAlignment="Left"
                                Margin="0,0,0,8"
                                ToolTip="Clear loaded file and reset">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Close" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Clear"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="{Binding LoadedFileName}"
                                   FontWeight="Medium"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8"
                                   Visibility="{Binding IsFileLoaded, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource MaterialDesignBody}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Audio Player Section -->
                    <StackPanel Grid.Column="1"
                                Visibility="{Binding IsFileLoaded, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Audio Player" 
                                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                                   Margin="0,0,0,8"/>

                        <!-- Playback Controls and Timeline -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Playback Controls -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                                <Button Command="{Binding PlayPauseCommand}"
                                        Style="{StaticResource MaterialDesignFloatingActionButton}"
                                        Width="40" Height="40"
                                        ToolTip="Play/Pause">
                                    <materialDesign:PackIcon Width="24" Height="24">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Kind" Value="Play"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                                        <Setter Property="Kind" Value="Pause"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>
                                </Button>

                                <Button Command="{Binding StopCommand}"
                                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                        Width="32" Height="32"
                                        Margin="8,0,0,0"
                                        ToolTip="Stop">
                                    <materialDesign:PackIcon Kind="Stop" Width="18" Height="18"/>
                                </Button>
                            </StackPanel>

                            <!-- Timeline -->
                            <Grid Grid.Column="1" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="{Binding CurrentPosition, Converter={StaticResource TimeSpanToStringConverter}}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"/>

                                <Slider Grid.Column="1"
                                        Minimum="0"
                                        Maximum="{Binding TotalDuration.TotalSeconds}"
                                        Value="{Binding SliderPosition, Mode=TwoWay}"
                                        materialDesign:SliderAssist.OnlyShowFocusVisualWhileDragging="True"
                                        VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="2" 
                                           Text="{Binding TotalDuration, Converter={StaticResource TimeSpanToStringConverter}}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Transcription Results -->
            <materialDesign:Card Grid.Row="2" Padding="16"
                                 Visibility="{Binding IsFileLoaded, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="350"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="Transcription Results" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   VerticalAlignment="Center"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button Command="{Binding ExportTranscriptionCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="0,0,8,0"
                                    Visibility="{Binding TranscriptionSegments.Count, Converter={StaticResource ZeroToVisibilityConverter}, ConverterParameter=Inverse}"
                                    ToolTip="Export transcription">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Export" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="Export"/>
                                </StackPanel>
                            </Button>
                            
                            <ToggleButton IsChecked="{Binding AutoScrollEnabled}"
                                          Style="{StaticResource MaterialDesignActionToggleButton}"
                                          ToolTip="Auto-scroll to current segment during playback">
                                <ToggleButton.Content>
                                    <materialDesign:PackIcon Kind="FormatVerticalAlignCenter"/>
                                </ToggleButton.Content>
                                <materialDesign:ToggleButtonAssist.OnContent>
                                    <materialDesign:PackIcon Kind="FormatVerticalAlignCenter"/>
                                </materialDesign:ToggleButtonAssist.OnContent>
                            </ToggleButton>
                        </StackPanel>
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="TranscriptScrollViewer">
                        <ItemsControl ItemsSource="{Binding TranscriptionSegments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="0,0,0,1"
                                            Padding="8"
                                            Margin="0,0,0,8"
                                            Cursor="Hand"
                                            x:Name="SegmentBorder">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                          Command="{Binding DataContext.SeekToSegmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <!-- Header: Speaker and Time Range -->
                                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="{Binding Speaker}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource SecondaryHueMidBrush}"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0"
                                                           Visibility="{Binding DataContext.ShowSpeakerLabels, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                
                                                <TextBlock FontWeight="Medium"
                                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                           VerticalAlignment="Center">
                                                    <Run Text="["/>
                                                    <Run Text="{Binding StartTime, Converter={StaticResource TimeSpanToStringConverter}}"/>
                                                    <Run Text=" - "/>
                                                    <Run Text="{Binding EndTime, Converter={StaticResource TimeSpanToStringConverter}}"/>
                                                    <Run Text="]"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <!-- Transcript Text -->
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding Text}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="1"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Visibility="{Binding TranscriptionSegments.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                        <materialDesign:PackIcon Kind="MessageText"
                                                 Width="64"
                                                 Height="64"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 HorizontalAlignment="Center"
                                                 Margin="0,0,0,16"/>
                        <TextBlock Text="No transcription yet"
                                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Click 'Start Transcription' to begin"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
